<%= form_for(@promo_slot) do |f| %>
  <% if @promo_slot.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@promo_slot.errors.count, "error") %> prohibited this promo_slot from being saved:</h2>

      <ul>
      <% @promo_slot.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :product_id %><br />
    <%= f.number_field :product_id %>
  </div>
  <div class="field">
    <%= f.label :slot_type %><br />
    <%= f.number_field :slot_type %>
  </div>
  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :link_url %><br />
    <%= f.text_field :link_url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
